package com.swiftvault.backend;

import org.junit.jupiter.api.Test;

/**
 * Basic sanity test — does NOT load full Spring context.
 * Full integration tests require a test DB — add later.
 */
class SwiftvaultBackendApplicationTests {

	@Test
	void contextLoads() {
		// Placeholder — Spring Boot integration tests added in Phase 3
	}
}